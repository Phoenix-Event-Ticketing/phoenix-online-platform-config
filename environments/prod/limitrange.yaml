apiVersion: v1
kind: LimitRange
metadata:
  name: phoenix-prod-limits
  namespace: phoenix-prod
  labels:
    app.kubernetes.io/part-of: phoenix-platform
    environment: prod
spec:
  limits:
    - type: Container
      default:
        cpu: 250m
        memory: 256Mi
      defaultRequest:
        cpu: 100m
        memory: 128Mi
      min:
        cpu: 50m
        memory: 64Mi
      max:
        cpu: 500m
        memory: 512Mi
