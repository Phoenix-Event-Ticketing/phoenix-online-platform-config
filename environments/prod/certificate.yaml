apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: phoenix-prod-gateway-tls
spec:
  secretName: phoenix-prod-gateway-tls
  dnsNames:
    - api.phoenix-project.online
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
