apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: booking-service-healthcheck
spec:
  default:
    checkIntervalSec: 10
    timeoutSec: 5
    healthyThreshold: 1
    unhealthyThreshold: 3
    config:
      type: HTTP
      httpHealthCheck:
        requestPath: /actuator/health/readiness
        portSpecification: USE_SERVING_PORT
  targetRef:
    group: ""
    kind: Service
    name: booking-service
