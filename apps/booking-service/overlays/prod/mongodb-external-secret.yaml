apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: booking-service-mongodb-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: phoenix-gcp
    kind: ClusterSecretStore
  target:
    name: booking-service-mongodb-secrets
    creationPolicy: Owner
  data:
    - secretKey: MONGO_INITDB_ROOT_PASSWORD
      remoteRef:
        key: REPLACE_GSM_BOOKING_MONGODB_ROOT_PASSWORD
